[package]
name = "validation_tests_generator"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
csv = "1.3"
clap = { version = "4.4", features = ["derive"] }
